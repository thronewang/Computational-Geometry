//Interval Intersection Detection

#include "utils.h"

//sign the points with left or right flags, sort them, the intervals intersect
//if and only if two left or two right flags appear consecutively.